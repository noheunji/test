extends base.pug

block content 
    form(method="POST", action="/exhibition/upload")
        input(type="text", placeholder="Title", required, name="title")
        input(type="submit", value="Upload")